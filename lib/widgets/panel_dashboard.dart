import 'package:flutter/material.dart';
import 'package:sliding_up_panel/sliding_up_panel.dart';

class panel_dashboard extends StatefulWidget {
  panel_dashboard({Key key}) : super(key: key);

  @override
  _panel_dashboardState createState() {
    return _panel_dashboardState();
  }
}

class _panel_dashboardState extends State<panel_dashboard> {
  @override
  void initState() {
    super.initState();
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    BorderRadiusGeometry radius = BorderRadius.only(
      topLeft: Radius.circular(24.0),
      topRight: Radius.circular(24.0),
    );
    // TODO: implement build
    return null;
  }
}